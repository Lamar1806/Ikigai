import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

export const RecoverPhraseScreen = () => {
  return (
    <View>
      <Text>RecoverPhraseScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

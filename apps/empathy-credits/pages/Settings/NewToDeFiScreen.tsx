import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

export const NewToDeFiScreen = () => {
  return (
    <View>
      <Text>NewToDeFiScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

{
  "scripts": {
    "build": "nx build true-self-vibs-web && yarn move-dist-folder && yarn deploy",
    "start": "nx serve true-self-vibs-web",
    "lint": "nx lint true-self-vibs-web",
    "test": "nx test true-self-vibs-web",
    "deploy": "firebase deploy",
    "move-dist-folder": "yarn rm-old-dist && mv ../../dist/apps/true-self-vibs-web ./dist",
    "rm-old-dist": "rm -rf ./dist"
  }
}
